<resources>

    <style name="AppTheme.Launcher">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceTheme</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.NoActionBar.TransparentStatusBar">
    </style>

    <style name="AppTheme.MaterialAboutActivity" parent="Theme.Mal.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PreferenceTheme" parent="PreferenceThemeOverlay">
        <!-- Only need this on pre-Lollipop -->
        <item name="accentColorCompat">?colorAccent</item>

        <item name="simpleMenuPreferenceStyle">@style/Preference.SimpleMenuPreference</item>
        <!--<item name="simpleMenuPreferenceStyle">@style/simpleMenuPreferenceStylePerfect</item>-->
        <!--<item name="switchPreferenceCompatStyle">@style/Preference.SwitchPreferenceCompat</item>-->
    </style>

    <style name="simpleMenuPreferenceStylePerfect" parent="@style/Preference.SimpleMenuPreference.Popup">
        <item name="listElevation">2dp</item>
        <item name="dialogElevation">2dp</item>
    </style>
</resources>
